void drawBuilding(){
  int leftx = 300;
  int bHeight = 250;
  int bWidth= 100;
  
  stroke(255);
  strokeWeight(4);
  
  //line(x1, y1, x2, y2)
  
  //walls
  line(leftx, height, leftx, bHeight);
  line(leftx+bWidth, height, leftx+bWidth, bHeight);
  
  //stories
  //for(int=0;
  
  
}
